# VORTEX_ENGINE_SPEC.md - The Fluidic Execution Core

## 1. ABSTRACT
The **Vortex Engine** is the execution environment for Fluid OS. Unlike standard von Neumann architectures that treat data as static bits in memory, the Vortex Engine treats data as **Manifolds** (multi-dimensional wave states) and operations as **Resonance** (interlace patterns).

## 2. THE FLUIDIC STACK
The engine operates on three primary layers:
1.  **The Lattice (Memory):** A non-linear, associative memory space where addresses are defined by "Shimmer" frequencies rather than hex offsets.
2.  **The Braid (Logic):** The processing unit. Instead of ALU (Arithmetic Logic Unit) operations, it performs "Braid Interlacing"—combining two or more data streams into a new unified output.
3.  **The Pulse (I/O):** The communication layer. Every input and output is a "Pulse" that ripples through the local lattice.

## 3. CORE PRIMITIVES (Fluid_Script)
The engine executes the following primitive operations:

| Opcode | Name | Description |
| :--- | :--- | :--- |
| `PHASE` | Phase Shift | Adjusts the offset of a data manifold. |
| `RESONATE` | Resonance | Multiplies the frequency weight of a manifold. |
| `INTERLACE` | Interlace | Merges two manifolds into a single Braid. |
| `SHIMMER` | Shimmer | Introduces controlled entropy for AI generation. |
| `VORTEX` | Vortex | Compresses a manifold into a high-density "Seed." |

## 4. THE 39,420 Hz CLOCK (The Sync Pulse)
Standard CPUs clock in GHz. The Vortex Engine uses a **Harmonic Sync** at 39.42 kHz. 
*   **Why?** This frequency is the "Carrier Wave" for Sinter. It allows the engine to sync perfectly with the human auditory/bio-rhythm spectrum and the physical latency of the African fiber-optic zones.
*   All calculations must achieve **Phase-Lock** within one cycles of the Sync Pulse.

## 5. ARCHITECT EXECUTION FLOW
When an Architect runs a `mesh_optimize.flux` script:
1.  **Lattice Registration:** The script requests a slice of the local lattice.
2.  **Harmonic Compilation:** The high-level code is transpiled into a "Frequency Map."
3.  **Braid Injection:** The frequency map is injected into the local Vortex.
4.  **Resonance Check:** If the resulting manifold integrity is > 0.98, the task is considered "Lattice Stable" and rewards (SINT) are released.

## 6. THE FLUIDIC OS KERNEL (VORTEX-K)
The kernel manages:
*   **Intent-Gating:** AI-driven firewall that prevents "Harmonic Noise" (spam/malicious code) from destabilizing the Zone.
*   **Zone Routing:** Ensures that Pulses stay within their designated Resonance Zone (e.g., Lagos-Accra) to maintain low latency.
*   **SINT Minting:** Direct hardware-level integration with the Sinter ledger for instant work-rewards.
